#= HYCATCH is modular therefore one can select modules which will run without effecting the others module_name
the different options

(1) run = run the module
(2) no = module not running
(3) file = data already stored in file

=#
module modules

   Snow = "run"

   Interception = "run"

end